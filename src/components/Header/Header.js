import React, { Component } from 'react';

class Header extends Component {
  render() {
    return (
      <header className="App-header">
        <h1 className="App-title">Movies!</h1>
        <p className="App-intro">Header needs moar Header</p>
      </header>
    );
  }
}

export default Header;
